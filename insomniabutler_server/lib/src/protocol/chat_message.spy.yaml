class: ChatMessage
table: chat_messages
fields:
  sessionId: String
  userId: int
  role: String
  content: String
  timestamp: DateTime
  embedding: Vector(768)?
indexes:
  chat_messages_embedding_idx:
    fields: embedding
    type: hnsw

